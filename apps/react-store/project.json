{
  "name": "react-store",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/react-store/src",
  "projectType": "application",
  "tags": ["type:app"],
  "// targets": "to see all targets run: nx show project react-store --web",
  "targets": {
    "deploy": {
      "dependsOn": ["lint", "test", "build"],
      "command": "gh-pages -d apps/react-store/build/react-store"
    }
  }
}
